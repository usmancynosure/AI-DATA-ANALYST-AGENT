# AI Analyst — Backend

FastAPI service: data layer, agent orchestrator, and sandboxed code execution.

See the [root README](../README.md) for the full picture. Quick start:

```bash
cd backend
uv sync --extra dev
uv run uvicorn app.main:app --reload
```
